/*eslint-env jquery*/
/* global videoList */
'use strict';
// When DOM is ready:
$(function () {
  // TASK:
  // 1. Run `handleFormSubmit` to bind the event listener to the DOM
  videoList.bindEventListeners();
});
