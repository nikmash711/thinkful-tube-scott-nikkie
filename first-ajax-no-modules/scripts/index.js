/*eslint-env jquery*/
/* global thinkfulTube */
'use strict';
// When DOM is ready:
$(function () {
  // TASK:
  // 1. Run `handleFormSubmit` to bind the event listener to the DOM
  thinkfulTube.bindEventListeners();

});
